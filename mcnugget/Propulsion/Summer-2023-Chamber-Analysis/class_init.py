import matplotlib.pyplot as plt
from pyfluids import Fluid, FluidsList, Input
import numpy as np
import pint


init = 1

class Regen_Channel:     
    L = init
    di = init
    do = init
    A = init
    dh = init
    k = init
    f = init

class Liner:     
    k = init
    rho = init
    a = init
    v = init
    E = init
    ty = init 
    tu = init
    di = init
    do = init
    T = init


