import matplotlib.pyplot as plt
from pyfluids import Fluid, FluidsList, Input

init = 1

class Regen_Channel:     
    L = init
    di = init
    do = init
    A = init
    dh = init
    k = init
    f = init

